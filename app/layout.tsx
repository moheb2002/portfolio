import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Moheb Adel - Microsoft Intern | AI Consultant | Copilot & Agentic AI Specialist",
  description: "Moheb Adel is an emerging AI consultant and Microsoft intern, specializing in Copilot and Agentic AI. Passionate about Generative AI, Azure AI, and enterprise transformation, Moheb bridges business strategy, customer success, and AI-powered innovation.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
